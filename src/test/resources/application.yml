spring:
  datasource:
    url: jdbc:h2:mem:powerup;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
